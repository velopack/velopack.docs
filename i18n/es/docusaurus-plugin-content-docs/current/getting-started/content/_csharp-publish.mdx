import Admonition from '@theme/Admonition';

Antes de crear una release de Velopack, primero debes compilar tu aplicación y publicarla en un directorio.


{!props.showMultipleTfmSuggestion && (
    <>
    <p>
    Para simplificar, recomendamos publicarla como una <a href="https://learn.microsoft.com/dotnet/core/deploying/#publish-self-contained">aplicación autocontenida</a>.
    </p>
    <p>
    <code className="language-bash">
    dotnet publish yourApp.csproj -c Release --self-contained -r win-x64 -o .\publish
    </code>
    </p>
    </>
)}


{props.showMultipleTfmSuggestion && (
    <>
        <p>
        <code className="language-cmd">
        dotnet publish -f net8.0-desktop -p:Version=1.0.0 -o .\publish
        </code>
        </p>
        <p>
        Esto creará los artefactos que se instalarán dentro del directorio <code>publish</code>. Si tienes como destino varias plataformas, este proceso deberá repetirse para cada una de ellas, reemplazando la opción <code>-f</code> por el identificador de framework de destino (TFM) apropiado para cada plataforma, como <code>net8.0-windows</code> o <code>net8.0-maccatalyst</code>.
        </p>
    </>
)}

{props.suffix && (
    <p>
        <span dangerouslySetInnerHTML={{ __html: props.suffix }} />
    </p>
)}

<Admonition type="tip">
    A partir de .NET 7, la opción `-o`/`--output` [ya no se puede usar con un archivo de solución][def].

    A partir de .NET 8 y posteriores, el comando `dotnet publish` usa por defecto la configuración Release, por lo que puedes omitir la opción `-c Release`.
    Para más detalles consulta https://learn.microsoft.com/en-us/dotnet/core/compatibility/sdk/8.0/dotnet-publish-config.

    Si ejecutas el comando dotnet publish desde el mismo directorio que el archivo .csproj, puedes omitir el argumento del proyecto. Puedes encontrar más detalles en la [documentación de dotnet publish](https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-publish?WT.mc_id=DT-MVP-5003472#arguments).
</Admonition>