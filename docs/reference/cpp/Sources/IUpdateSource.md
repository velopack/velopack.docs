---
title: IUpdateSource
sidebar_label: IUpdateSource
sidebar_position: 2
---
<!-- auto-generated by generator/ — do not edit -->

# IUpdateSource class

Namespace: [`Sources`](./index.md)

Abstract class for custom update sources. Subclass this and implement GetReleaseFeed and DownloadReleaseEntry to provide a custom way of fetching releases and downloading assets.

```cpp
class IUpdateSource :public IUpdateSourcePointer
```

## Remarks

SAFETY: It is your responsibility to ensure that a derived class instance is thread-safe, as Velopack may call methods on this class from multiple threads.

## Constructors

| Constructor | Summary |
| --- | --- |
| [`IUpdateSource`](#velopack-iupdatesource-iupdatesource) | Default constructor for IUpdateSource. This will create a new custom source that calls back into the virtual methods of this class. |


### IUpdateSource() {#velopack-iupdatesource-iupdatesource}

```cpp
IUpdateSource();
```

Default constructor for IUpdateSource. This will create a new custom source that calls back into the virtual methods of this class.

## Methods

| Method | Summary |
| --- | --- |
| [`GetReleaseFeed`](#velopack-iupdatesource-getreleasefeed-std-stringconst) | Fetches the release feed json for the specified releases name, and returns it as a string. |
| [`DownloadReleaseEntry`](#velopack-iupdatesource-downloadreleaseentry-velopack-velopackassetconst-std-stringconst-velopack-vpkc-progress-send-t) | Downloads an asset to the specified local file path. Progress is reported back to Velopack via a callback. |


### GetReleaseFeed() {#velopack-iupdatesource-getreleasefeed-std-stringconst}

```cpp
virtual std::string const GetReleaseFeed(std::string const releasesName) = 0;
```

Fetches the release feed json for the specified releases name, and returns it as a string.

### DownloadReleaseEntry() {#velopack-iupdatesource-downloadreleaseentry-velopack-velopackassetconst-std-stringconst-velopack-vpkc-progress-send-t}

```cpp
virtual bool DownloadReleaseEntry(Velopack::VelopackAsset const& asset, std::string const localFilePath, Velopack::vpkc_progress_send_t progress) = 0;
```

Downloads an asset to the specified local file path. Progress is reported back to Velopack via a callback.

___
*Generated from `Velopack (C++ headers)`*
