---
title: UpdateInfo
sidebar_label: UpdateInfo
sidebar_position: 3
---
<!-- auto-generated by generator/ — do not edit -->

# UpdateInfo struct

Holds information about the current version and pending updates, such as how many there are, and access to release notes.

```cpp
struct UpdateInfo
```

## Fields

| Field | Summary |
| --- | --- |
| [`IsDowngrade`](#velopack-updateinfo-isdowngrade) | True if the update is a version downgrade or lateral move (such as when switching channels to the same version number). In this case, only full updates are allowed, and any local packages on disk newer than the downloaded version will be deleted. |
| [`TargetFullRelease`](#targetfullrelease) | The available version that we are updating to. |
| [`BaseRelease`](#baserelease) | The base release that this update is based on. This is only available if the update is a delta update. |
| [`DeltasToTarget`](#deltastotarget) | The list of delta updates that can be applied to the base version to get to the target version. |


### IsDowngrade {#velopack-updateinfo-isdowngrade}

```cpp
bool IsDowngrade;
```

True if the update is a version downgrade or lateral move (such as when switching channels to the same version number). In this case, only full updates are allowed, and any local packages on disk newer than the downloaded version will be deleted.

### TargetFullRelease {#targetfullrelease}

The available version that we are updating to.

### BaseRelease {#baserelease}

The base release that this update is based on. This is only available if the update is a delta update.

### DeltasToTarget {#deltastotarget}

The list of delta updates that can be applied to the base version to get to the target version.

___
*Generated from `Velopack (C++ headers)`*
