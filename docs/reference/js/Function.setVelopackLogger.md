# Function: setVelopackLogger()

> **setVelopackLogger**(`callback`): `void`

Set a custom logger callback to receive log messages from Velopack. The default behavior is to log to console.log.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `callback` | (`loglevel`, `msg`) => `void` |

## Returns

`void`
