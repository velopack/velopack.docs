---
title: IVelopackLocator
sidebar_label: IVelopackLocator
sidebar_position: 4
---
<!-- auto-generated by generator/ — do not edit -->

# IVelopackLocator interface

Namespace: [`Velopack.Locators`](./index.md)

An interface describing where Velopack can find key folders and files.

```cs
public interface IVelopackLocator
```

## Properties

| Property | Summary |
| --- | --- |
| [`AppId`](#appid) | The unique application Id. This is used in various app paths. |
| [`RootAppDir`](#rootappdir) | The root directory of the application. On Windows, this folder contains all |
| [`PackagesDir`](#packagesdir) | The directory in which nupkg files are stored for this application. |
| [`AppContentDir`](#appcontentdir) | The directory in which versioned application files are stored. |
| [`AppTempDir`](#apptempdir) | The temporary directory for this application. |
| [`UpdateExePath`](#updateexepath) | The path to the current Update.exe or similar on other operating systems. |
| [`CurrentlyInstalledVersion`](#currentlyinstalledversion) | The currently installed version of the application, or null if the app is not installed. |
| [`ThisExeRelativePath`](#thisexerelativepath) | The path from `AppContentDir` to this executable. |
| [`Channel`](#channel) | The release channel this package was built for. |
| [`AppUserModelId`](#appusermodelid) | The Application User Model ID for this app, used for taskbar grouping and toast notifications on Windows. |
| [`Log`](#log) | The logging interface to use for Velopack diagnostic messages. |
| [`IsPortable`](#isportable) | A flag indicating if this is a portable build, and that the settings should be self-contained in the package. |
| [`Process`](#process) | Provides an abstraction for dealing with the dotnet Process API. This is used to start processes and |


### AppId {#appid}

```cs
string? AppId { get; }
```

The unique application Id. This is used in various app paths.

### RootAppDir {#rootappdir}

```cs
string? RootAppDir { get; }
```

The root directory of the application. On Windows, this folder contains all 
the application files, but that may not be the case on other operating systems.

### PackagesDir {#packagesdir}

```cs
string? PackagesDir { get; }
```

The directory in which nupkg files are stored for this application.

### AppContentDir {#appcontentdir}

```cs
string? AppContentDir { get; }
```

The directory in which versioned application files are stored.

### AppTempDir {#apptempdir}

```cs
string? AppTempDir { get; }
```

The temporary directory for this application.

### UpdateExePath {#updateexepath}

```cs
string? UpdateExePath { get; }
```

The path to the current Update.exe or similar on other operating systems.

### CurrentlyInstalledVersion {#currentlyinstalledversion}

```cs
SemanticVersion? CurrentlyInstalledVersion { get; }
```

The currently installed version of the application, or null if the app is not installed.

### ThisExeRelativePath {#thisexerelativepath}

```cs
string? ThisExeRelativePath { get; }
```

The path from `AppContentDir` to this executable.

### Channel {#channel}

```cs
string? Channel { get; }
```

The release channel this package was built for.

### AppUserModelId {#appusermodelid}

```cs
string? AppUserModelId { get; }
```

The Application User Model ID for this app, used for taskbar grouping and toast notifications on Windows.

### Log {#log}

```cs
IVelopackLogger Log { get; }
```

The logging interface to use for Velopack diagnostic messages.

### IsPortable {#isportable}

```cs
bool IsPortable { get; }
```

A flag indicating if this is a portable build, and that the settings should be self-contained in the package.
On Windows, this is true for portable builds, and false for non-portable builds which were installed by Setup.exe
On OSX and Linux, this is always false, because settings and application files should be stored in the user's 
home directory.

### Process {#process}

```cs
IProcessImpl Process { get; }
```

Provides an abstraction for dealing with the dotnet Process API. This is used to start processes and
get information about the current process.

## Methods

| Method | Summary |
| --- | --- |
| [`AddLogger`](#addlogger-ivelopacklogger) | Add a logger to the list of loggers. This will be used to log messages from Velopack. |
| [`GetLocalPackages`](#getlocalpackages) | Finds .nupkg files in the PackagesDir and returns a list of ReleaseEntryName objects. |
| [`GetLatestLocalFullPackage`](#getlatestlocalfullpackage) | Finds latest .nupkg file in the PackagesDir or null if not found. |
| [`GetOrCreateStagedUserId`](#getorcreatestageduserid) | Unique identifier for this user which is used to calculate whether this user is eligible for |


### AddLogger(logger) {#addlogger-ivelopacklogger}

```cs
void AddLogger(IVelopackLogger logger)
```

Add a logger to the list of loggers. This will be used to log messages from Velopack.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `logger` | [`IVelopackLogger`](../Velopack.Logging/IVelopackLogger.md) |  |


### GetLocalPackages() {#getlocalpackages}

```cs
List<VelopackAsset> GetLocalPackages()
```

Finds .nupkg files in the PackagesDir and returns a list of ReleaseEntryName objects.

**Returns** `List<VelopackAsset>`

### GetLatestLocalFullPackage() {#getlatestlocalfullpackage}

```cs
VelopackAsset? GetLatestLocalFullPackage()
```

Finds latest .nupkg file in the PackagesDir or null if not found.

**Returns** [`VelopackAsset`](../Velopack/VelopackAsset.md)

### GetOrCreateStagedUserId() {#getorcreatestageduserid}

```cs
Guid? GetOrCreateStagedUserId()
```

Unique identifier for this user which is used to calculate whether this user is eligible for 
staged roll outs.

**Returns** `Guid?`

___
*Generated from `Velopack` 1.1.1*
