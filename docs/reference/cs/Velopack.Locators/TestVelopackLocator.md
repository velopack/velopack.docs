---
title: TestVelopackLocator
sidebar_label: TestVelopackLocator
sidebar_position: 2
---
<!-- auto-generated by generator/ — do not edit -->

# TestVelopackLocator class

Namespace: [`Velopack.Locators`](./index.md)

Provides a mock / test implementation of [`VelopackLocator`](./VelopackLocator.md). This can be used to verify that
your application is able to find and prepare updates from your chosen update source without actually
having an installed application. This could be used in a CI/CD pipeline, or unit tests etc.

```cs
[ExcludeFromCodeCoverage]
public class TestVelopackLocator : VelopackLocator, IVelopackLocator, IProcessImpl
```

**Inherits:** [`VelopackLocator`](./VelopackLocator.md)  
**Implements:** [`IVelopackLocator`](./IVelopackLocator.md), [`IProcessImpl`](./IProcessImpl.md)  

## Constructors

| Constructor | Summary |
| --- | --- |
| [`TestVelopackLocator`](#testvelopacklocator-string-string-string-ivelopacklogger) | Provides a mock / test implementation of `VelopackLocator`. This can be used to verify that |
| [`TestVelopackLocator`](#testvelopacklocator-string-string-string-string-string-string-string-ivelopacklogger-velopackasset-string) | Provides a mock / test implementation of `VelopackLocator`. This can be used to verify that |


### TestVelopackLocator(appId, version, packagesDir, logger) {#testvelopacklocator-string-string-string-ivelopacklogger}

```cs
public TestVelopackLocator(string appId, string version, string packagesDir, IVelopackLogger? logger = null)
```

Provides a mock / test implementation of [`VelopackLocator`](./VelopackLocator.md). This can be used to verify that
your application is able to find and prepare updates from your chosen update source without actually
having an installed application. This could be used in a CI/CD pipeline, or unit tests etc.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `appId` | `string` |  |
| `version` | `string` |  |
| `packagesDir` | `string` |  |
| `logger` | [`IVelopackLogger`](../Velopack.Logging/IVelopackLogger.md) |  |


### TestVelopackLocator(appId, version, packagesDir, appDir, rootDir, updateExe, channel, logger, localPackage, processPath) {#testvelopacklocator-string-string-string-string-string-string-string-ivelopacklogger-velopackasset-string}

```cs
public TestVelopackLocator(string appId, string version, string packagesDir, string? appDir, string? rootDir, string? updateExe, string? channel = null, IVelopackLogger? logger = null, VelopackAsset? localPackage = null, string processPath = null)
```

Provides a mock / test implementation of [`VelopackLocator`](./VelopackLocator.md). This can be used to verify that
your application is able to find and prepare updates from your chosen update source without actually
having an installed application. This could be used in a CI/CD pipeline, or unit tests etc.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `appId` | `string` |  |
| `version` | `string` |  |
| `packagesDir` | `string` |  |
| `appDir` | `string` |  |
| `rootDir` | `string` |  |
| `updateExe` | `string` |  |
| `channel` | `string` |  |
| `logger` | [`IVelopackLogger`](../Velopack.Logging/IVelopackLogger.md) |  |
| `localPackage` | [`VelopackAsset`](../Velopack/VelopackAsset.md) |  |
| `processPath` | `string` |  |


## Properties

| Property | Summary |
| --- | --- |
| [`AppId`](#appid) | The unique application Id. This is used in various app paths. |
| [`RootAppDir`](#rootappdir) | The root directory of the application. On Windows, this folder contains all |
| [`PackagesDir`](#packagesdir) | The directory in which nupkg files are stored for this application. |
| [`UpdateExePath`](#updateexepath) | The path to the current Update.exe or similar on other operating systems. |
| [`CurrentlyInstalledVersion`](#currentlyinstalledversion) | The currently installed version of the application, or null if the app is not installed. |
| [`AppContentDir`](#appcontentdir) | The directory in which versioned application files are stored. |
| [`Channel`](#channel) | The release channel this package was built for. |
| [`Log`](#log) | The logging interface to use for Velopack diagnostic messages. |
| [`Process`](#process) | Provides an abstraction for dealing with the dotnet Process API. This is used to start processes and |
| [`ExitCode`](#exitcode) |  |


### AppId {#appid}

```cs
public override string? AppId { get; }
```

The unique application Id. This is used in various app paths.

### RootAppDir {#rootappdir}

```cs
public override string? RootAppDir { get; }
```

The root directory of the application. On Windows, this folder contains all 
the application files, but that may not be the case on other operating systems.

### PackagesDir {#packagesdir}

```cs
public override string? PackagesDir { get; }
```

The directory in which nupkg files are stored for this application.

### UpdateExePath {#updateexepath}

```cs
public override string? UpdateExePath { get; }
```

The path to the current Update.exe or similar on other operating systems.

### CurrentlyInstalledVersion {#currentlyinstalledversion}

```cs
public override SemanticVersion? CurrentlyInstalledVersion { get; }
```

The currently installed version of the application, or null if the app is not installed.

### AppContentDir {#appcontentdir}

```cs
public override string? AppContentDir { get; }
```

The directory in which versioned application files are stored.

### Channel {#channel}

```cs
public override string? Channel { get; }
```

The release channel this package was built for.

### Log {#log}

```cs
public override IVelopackLogger Log { get; }
```

The logging interface to use for Velopack diagnostic messages.

### Process {#process}

```cs
public override IProcessImpl Process { get; }
```

Provides an abstraction for dealing with the dotnet Process API. This is used to start processes and
get information about the current process.

### ExitCode {#exitcode}

```cs
public int? ExitCode { get; }
```

## Methods

| Method | Summary |
| --- | --- |
| [`GetLatestLocalFullPackage`](#getlatestlocalfullpackage) | Finds latest .nupkg file in the PackagesDir or null if not found. |
| [`GetCurrentProcessPath`](#getcurrentprocesspath) | Gets the full path to the current process executable. |
| [`GetCurrentProcessId`](#getcurrentprocessid) | Gets the process ID of the current process. |
| [`StartProcess`](#startprocess-string-ienumerable-string-string-bool) | Starts a new process with the specified executable path, arguments, and options. |
| [`Exit`](#exit-int) | Exit the current process with the given exit code. |


### GetLatestLocalFullPackage() {#getlatestlocalfullpackage}

```cs
public override VelopackAsset? GetLatestLocalFullPackage()
```

Finds latest .nupkg file in the PackagesDir or null if not found.

**Returns** [`VelopackAsset`](../Velopack/VelopackAsset.md)

### GetCurrentProcessPath() {#getcurrentprocesspath}

```cs
public string GetCurrentProcessPath()
```

Gets the full path to the current process executable.

**Returns** `string` — The full path to the current process executable.

### GetCurrentProcessId() {#getcurrentprocessid}

```cs
public uint GetCurrentProcessId()
```

Gets the process ID of the current process.

**Returns** `uint` — The process ID of the current process.

### StartProcess(exePath, args, workDir, showWindow) {#startprocess-string-ienumerable-string-string-bool}

```cs
public void StartProcess(string exePath, IEnumerable<string> args, string workDir, bool showWindow)
```

Starts a new process with the specified executable path, arguments, and options.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `exePath` | `string` | The path to the executable to start. |
| `args` | `IEnumerable<string>` | The command-line arguments to pass to the process. |
| `workDir` | `string` | The working directory for the new process. |
| `showWindow` | `bool` | Whether to show a window for the new process. |


### Exit(exitCode) {#exit-int}

```cs
public void Exit(int exitCode)
```

Exit the current process with the given exit code.

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `exitCode` | `int` | The exit code |


___
*Generated from `Velopack` 1.2.0*
